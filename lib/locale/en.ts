import en from 'date-format-parse/es/locale/en';

const lang = {
  formatLocale: en,
  yearFormat: 'YYYY',
  monthFormat: 'MMM',
  monthBeforeYear: true,
  buddhistYear: false,
};

export default lang;
